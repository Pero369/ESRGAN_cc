# 第4章实验补充 — 待完成步骤

## 第一步：安装依赖

```bash
pip install lpips
```

---

## 第二步：评估最优模型（Set5 / Set14）

```bash
# Set5
python evaluate.py \
  --model_path ./checkpoints/GRADIENT_LOSS_EXPERIMENTS/grad0.1/generator_gan_150.pth \
  --dataset_dir ./data/Set5 \
  --output_csv ./results/.csv/eval_set5_ychannel.csv \
  --y_channel

# Set14
python evaluate.py \
  --model_path ./checkpoints/GRADIENT_LOSS_EXPERIMENTS/grad0.1/generator_gan_150.pth \
  --dataset_dir ./data/Set14 \
  --output_csv ./results/.csv/eval_set14_ychannel.csv \
  --y_channel
```

结果填入位置：第4章各对比表，补充 Set5/Set14 分数据集一列。

---

## 第三步：量化模型对比实验

```bash
python quantization/eval_quantized.py
```

结果保存到 `results/.csv/eval_quantization.csv`，用于替换表4-5中所有 `~` 估算值。

---

## 第四步：补充 grad=0.15 实验

1. 修改 `config.py`：
   ```python
   enable_gradient_loss = True
   lambda_gradient = 0.15
   ```
2. 修改 `Config.checkpoint_dir` 指向新目录，避免覆盖已有权重：
   ```python
   checkpoint_dir = './checkpoints/GRADIENT_LOSS_EXPERIMENTS/grad0.15'
   ```
3. 运行训练：
   ```bash
   python train.py
   ```
4. 训练完成后评估：
   ```bash
   python evaluate.py \
     --model_path ./checkpoints/GRADIENT_LOSS_EXPERIMENTS/grad0.15/generator_gan_150.pth \
     --dataset_dir ./data/Set5 \
     --y_channel
   ```
5. 将结果补充到表4-3（梯度损失权重对比）和图4-2（倒U型曲线）。

---

## 第五步：画训练曲线图（可选，需重新训练后才有日志）

训练完成后 `logs/` 目录会生成：
- `psnr_stage.csv`：PSNR预训练阶段 loss 曲线
- `gan_stage.csv`：GAN阶段 G/D loss 曲线

用这两个文件画图，插入论文 4.2 节训练策略后面。

---

## 完成后需更新的文档位置

| 数据 | 填入位置 |
|------|---------|
| Set5/Set14 PSNR/SSIM/LPIPS | 第4章各实验对比表新增列 |
| 量化真实数据 | 表4-5（替换所有 `~` 值） |
| grad=0.15 结果 | 表4-3 新增一行，图4-2 新增一个点 |
| 训练曲线图 | 4.2节后新增图4-0（或附录） |
